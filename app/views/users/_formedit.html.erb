<%= form_for current_user, layout: :basic do |f| %>
<% if current_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% current_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.fieldset 'Required data' do %>
  <%= f.text_field :name %>
  <%= f.text_field :last_name %>
  <%= f.email_field :email %>

  <%= f.date_field :birthdate, max: '1996-01-01' %>
  <% end %>

  <div class="forgotpw">forgot password?</div>  
  <div class="forgot">
  <%= f.fieldset 'Password reset' do %>
  <%= f.password_field :password %>
  <%= f.password_field :password_confirmation %>
  <% end %>
  </div></br>

    <%= f.fieldset 'Optional data' do %>
    <%= f.phone_field :mobile %>
    <%= f.url_field :picture %>
  <% end %>
  <div class="actions"><%= f.submit %></div>
<% end %>
<% if current_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% current_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>