<p id="notice"><%= notice %></p>

<div class="col-md-4" id="leftside">

  <%= render "maps/leftside_map" %>

</div>
<div class="col-md-6" id="middle">

  <div id="class_post">
    <h1>Listing Klasses</h1>
    <% @klasses.each do |klass| %>
      <li>
          <div class="dealbox">
              <div id="dealheader">
                  <%= klass.title %>
              </div>
                 <div class="dealpad">
                    <div class="col-md-6">
                      <%= klass.place %><br>
                      <%= klass.date %>
                    </div>  
                     <div class="col-md-6">

                      <%= button_to "From: #{klass.price}₪", klass, :method => "get" %>
              Session
                  </div>
              </div>
          </div>
      </li>
    <% end %>

  </div>


<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Teacher</th>
      <th>Description</th>
      <th>Date</th>
      <th>Time</th>
      <th>Place</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @klasses.each do |klass| %>
      <tr>
        <td><%= klass.title %></td>
        <td><%= klass.teacher %></td>
        <td><%= klass.description %></td>
        <td><%= klass.date %></td>
        <td><%= klass.time %></td>
        <td><%= klass.place %></td>
        <td><%= klass.price %></td>
        <td><%= link_to 'Show', klass %></td>
        <td><%= link_to 'Edit', edit_klass_path(klass) %></td>
        <td><%= link_to 'Destroy', klass, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Klass', new_klass_path %>

</div>
<div class="col-md-2">
</div>
